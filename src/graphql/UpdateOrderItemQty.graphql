mutation UpdateOrderItemQty($orderId: ID, $orderItemId: ID!, $quantity: Int!) {
	updateOrderItemQty(orderId: $orderId, orderItemId: $orderItemId, quantity: $quantity) {
		id
		quantity
		product {
			name
		}
	}
}
