query CategoryGetList($pagination: PaginationInput!) {
	category(pagination: $pagination) {
		data {
			id
			name
			slug
			product {
				name
				id
				slug
			}
		}
		pagination {
			pages
			total
		}
	}
}
